import { Canvas, Meta, Source, Stories } from '@storybook/blocks';

import * as FlexBoxStories from './flex-box.stories';

<Meta of={FlexBoxStories} />

# FlexBoxStories

A `FlexBox` container is a component that allows you to lay out items in a row or column. It is a powerful layout tool that allows you to align items in a predictable way.

Use `FlexBox` together with `FlexBox.Item` as children to create flexbox layouts.

<Stories />
